<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Đăng Nhập</title>
    <script src="https://www.google.com/recaptcha/enterprise.js" async defer></script>
    <style>
        .login-container {
            color: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            max-width: 400px;
            width: 100%;
            margin: 0 auto;
            border: 2px solid rgb(14, 14, 14);  
            background-color: rgba(2, 2, 2, 0.5);
        }
        body {
            background-image: url('https://img6.thuthuatphanmem.vn/uploads/2022/04/15/anh-nen-ke-sach-dep-nhat_022005271.jpg');
            background-size: cover; /* Giúp hình nền bao phủ toàn bộ màn hình */
            background-repeat: no-repeat; /* Để hình nền không lặp lại */
            background-position: center; /* Định vị hình nền ở giữa */
        }
    </style>
</head>
<body>
    <div class="login-container my-3">
        <h3 class="text-center">Đăng Nhập</h3>
        <form action="/login/checklogin" method="POST">
            <div class="mb-3">
                <label for="email" class="form-label">Email</label>
                <input type="email" class="form-control" id="email" name="email" placeholder="Nhập email của bạn" required pattern="[a-zA-Z0-9._%+-]+@gmail\.com">
            </div>
            <div class="mb-3">
                <label for="password" class="form-label">Mật khẩu</label>
                <input type="password" class="form-control" id="password" name="password" placeholder="Nhập mật khẩu" required maxlength="100" minlength="8">
            </div>
            <p class="text-left">
                <a style="color: red;" href="#">Quên mật khẩu?</a>
            </p>
            {{!-- <div class="g-recaptcha mb-3 my-3" data-sitekey="6LeI1wMrAAAAANclLs0c9QC7jETXHZYrPZtu6-CO" data-action="LOGIN"></div> --}}
            <button type="submit" class="btn btn-primary w-100 mb-4">Đăng Nhập</button>
        </form>
        <a class="text-center btn btn-primary w-100" style="color: rgb(248, 248, 248); text-decoration: none;" href="/register">Tạo tài khoản mới</a>
    </div>
</body>
</html>
