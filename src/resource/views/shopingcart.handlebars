<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Giỏ hàng</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
            background-color: #f9f9f9;
        }

        main {
            width: 80%;
            margin: auto;
            background: white;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
        }

        h2 {
            text-align: center;
            color: #333;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        th,
        td {
            border: 1px solid #ddd;
            padding: 10px;
            text-align: center;
        }

        th {
            background-color: #4CAF50;
            color: white;
        }

        img {
            border-radius: 5px;
        }

        .cart-summary {
            margin-top: 20px;
            text-align: right;
            font-size: 18px;
        }

        .checkout-btn {
            display: inline-block;
            padding: 10px 20px;
            background-color: #ff5722;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            font-weight: bold;
        }

        .checkout-btn:hover {
            background-color: #e64a19;
        }
    </style>
</head>

<body>
    <main>
        <h2>Giỏ hàng của bạn</h2>
        <table>
            <thead>
                <tr>
                    <th>Tên sách</th>
                    <th>Số lượng</th>
                    <th>Thời gian mượn</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                {{#each borrows}}
                <tr>
                    <td>{{BookName}}</td>
                    <td>1</td>
                    <td>1 tháng</td>
                    <td>
                        <form action="/shopingcart/delete" method="post" style="display: inline;">
                            <input type="hidden" name="BookID" value="{{BookID}}">
                            <button type="submit" class="btn btn-danger">Xóa</button>
                        </form>
                    </td>
                </tr>
                {{/each}}
            </tbody>
        </table>

        <div class="cart-summary">
            <form action="/shopingcart/checkout" method="post">
                <button type="submit" class="checkout-btn">Mượn</button>
            </form>
        </div>
    </main>
</body>

</html>