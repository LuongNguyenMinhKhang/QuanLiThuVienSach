<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Đăng Ký Tài Khoản</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .register-container {
            color: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            max-width: 400px;
            width: 100%;
            margin: 0 auto;
            border: 2px solid rgb(14, 14, 14);  
            background-color: rgba(2, 2, 2, 0.5);
        }
        body {
            background-image: url('https://img6.thuthuatphanmem.vn/uploads/2022/04/15/anh-nen-ke-sach-dep-nhat_022005271.jpg');
            background-size: cover; /* Giúp hình nền bao phủ toàn bộ màn hình */
            background-repeat: no-repeat; /* Để hình nền không lặp lại */
            background-position: center; /* Định vị hình nền ở giữa */
        }
    </style>
</head>
<body>
    <div class="register-container">
        <h3 class="text-center">Đăng Ký</h3>
        <form id="form-rg"class="mt-4" method="post" action="/register/RegisterPost">
            {{!-- <div class="mb-3">
                <label for="username" class="form-label">Họ và tên</label>
                <input type="text" class="form-control" id="username" placeholder="Nhập tên tài khoản" required maxlength="100" minlength="8" pattern="^[a-zA-ZÀÁÂÃÈÉÊÌÍÒÓÔÕÙÚĂĐĨŨƠàáâãèéêìíòóôõùúăđĩũơƯĂẰẮẴẶÂẦẤẨẬÊỀẾỂỄỆÔỒỐỔỖỘƠỜỚỞỠỢÙÚỦỤỪỨỬỮỰỲÝỶỸỴýỳỷỹỵ\s]+$">
            </div> --}}
            <div class="mb-3">
                <label for="email" class="form-label">Email</label>
                <input type="email" class="form-control" id="email" name="email" placeholder="Nhập email" required>
            </div>
            <div class="mb-3">
                <p class="btn w-100" style="border: 1px solid white; color: white;" onclick="alert('Nút thêm thông tin đã tồn tại');">Thêm thông tin</p>
            </div>
            <div class="mb-3">
                <label for="password" class="form-label">Mật khẩu</label>
                <input type="password" class="form-control" id="password" name="password" placeholder="Nhập mật khẩu" maxlength="100" minlength="8" required>
            </div>
            <div class="mb-3">
                <div id="error-message" style="color: red; display: none;"></div>
                <label for="confirm-password" class="form-label">Xác nhận mật khẩu</label>
                <input type="password" class="form-control" id="confirm-password" placeholder="Nhập lại mật khẩu" maxlength="100" minlength="8" re>
            </div>
            <button type="submit" class="btn btn-primary w-100">Đăng Ký</button>
        </form>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        document.getElementById('form-rg').addEventListener('submit', function(event) {
            const password = document.getElementById('password').value;
            const confirmPassword = document.getElementById('confirm-password').value;

            const errorMessage = document.getElementById('error-message'); // Reference the error message element

            if (password !== confirmPassword) {
                event.preventDefault(); // Prevent form submission
                errorMessage.textContent = 'Mật khẩu và xác nhận mật khẩu không khớp!'; // Update error message
                errorMessage.style.display = 'block'; // Show error message
            } else {
                errorMessage.style.display = 'none'; // Hide error message if valid
                console.log('Đăng ký thành công!'); // For debugging
            }
        });
    </script>
</body>
</html>